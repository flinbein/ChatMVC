name: ChatMVC
version: ${plugin-version}
description: ${plugin-description}
api-version: ${spigot-api-version}
authors: [DPOH-VAR, MYXOMOPX]
database: false
main: ru.flinbein.chatmvc.ChatMVCPlugin

libraries:
  - org.freemarker:freemarker:${freemarker-version}
  - net.bytebuddy:byte-buddy:${bytebuddy-version}

commands:
  chatmvc:
    description: Service command of built-in CommandHandler
    usage: This commands used from chat requests only